@page
@model Vidhyalaya.Pages_Guardians.IndexModel

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Guardian[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Guardian[0].Address)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Guardian[0].PhoneNumber)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Guardian) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Address)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PhoneNumber)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.GuardianId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.GuardianId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.GuardianId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

